node_modules/
ui/dist/
*.log
.DS_Store
out/

# Personal working notes — reports, drafts. Not part of the published project.
docs/

# Local captures and exports, plus anything npm pack leaves behind.
.curlapi/
*.tgz
curls.sh
api-notes.md
session.json

# Handed-over API documents used for parser development. They carry real keys
# and credentials, so they stay local.
Doc samples/

# Icons are drawn by scripts/make-icons.mjs, so the bytes are a build output
# rather than a source. `npm run build:icons` recreates them exactly.
assets/*.png
assets/*.icns
assets/*.ico

# Release payloads, built by scripts/build-payload.sh
dist/
